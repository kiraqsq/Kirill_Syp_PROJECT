= form_tag("/display_theme", remote: true, :format => "js") do
  = select_tag :theme,
  options_for_select(@themes, selected: @selected_theme),
  prompt: t('.select_theme'),
  onchange:  "$(this).parent('form').submit();",
  formats: [ :js ],
  style: "appearance: none; -webkit-appearance: none; -moz-appearance: none; cursor: pointer; width: 60%;",
  class: "button btn btn-outline-dark rounded-pill m-2 d-block"